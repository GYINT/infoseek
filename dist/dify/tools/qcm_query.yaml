# 工具定义（v1.0.1 G8 自动生成，与 MCP TOOLS 对齐）
identity:
  name: qcm_query
  author: infoseek
  label:
    en_US: qcm_query
    zh_Hans: qcm_query
  description:
    human:
      en_US: 反向调用 QCM：质量危机问题的 4 形态输出（case_application/decision_card/assessment_report/quick_response）。QCM 未安装时优雅降级返回 degraded。
      zh_Hans: 反向调用 QCM：质量危机问题的 4 形态输出（case_application/decision_card/assessment_report/quick_response）。QCM 未安装时优雅降级返回 degraded。
    llm: 反向调用 QCM：质量危机问题的 4 形态输出（case_application/decision_card/assessment_report/quick_response）。QCM 未安装时优雅降级返回 degraded。
parameters:
  - name: query
    type: string
    required: true
    label:
      en_US: query
      zh_Hans: query
    human_description:
      en_US: 质量问题描述（必填）
      zh_Hans: 质量问题描述（必填）
    llm_description: 质量问题描述（必填）
    form: llm
  - name: form
    type: string
    required: false
    label:
      en_US: form
      zh_Hans: form
    human_description:
      en_US: 输出形态（可选）
      zh_Hans: 输出形态（可选）
    llm_description: 输出形态（可选）
    form: llm
extra:
  python:
    source: tools/qcm_query.py