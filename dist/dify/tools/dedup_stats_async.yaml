# 工具定义（v1.0.1 G8 自动生成，与 MCP TOOLS 对齐）
identity:
  name: dedup_stats_async
  author: infoseek
  label:
    en_US: dedup_stats_async
    zh_Hans: dedup_stats_async
  description:
    human:
      en_US: （v1.0.1 由 sync 定义生成，schema 字段描述完整）任务报告：URL 总数、主题分布、抓取时间统计。
      zh_Hans: （v1.0.1 由 sync 定义生成，schema 字段描述完整）任务报告：URL 总数、主题分布、抓取时间统计。
    llm: （v1.0.1 由 sync 定义生成，schema 字段描述完整）任务报告：URL 总数、主题分布、抓取时间统计。
parameters:
  - name: subject
    type: string
    required: false
    label:
      en_US: subject
      zh_Hans: subject
    human_description:
      en_US: 可选，限定主题
      zh_Hans: 可选，限定主题
    llm_description: 可选，限定主题
    form: llm
extra:
  python:
    source: tools/dedup_stats_async.py