# 工具定义（v1.0.1 G8 自动生成，与 MCP TOOLS 对齐）
identity:
  name: conflict_detection_async
  author: infoseek
  label:
    en_US: conflict_detection_async
    zh_Hans: conflict_detection_async
  description:
    human:
      en_US: （v1.0.1 由 sync 定义生成，schema 字段描述完整）跨源事实冲突检测 (v1.8.1+ 第 9 工具)。输入多个来源（含 text/title/url/score），自动识别对同一实体的不同表述/数值，按严重度排序输出冲突列表。
      zh_Hans: （v1.0.1 由 sync 定义生成，schema 字段描述完整）跨源事实冲突检测 (v1.8.1+ 第 9 工具)。输入多个来源（含 text/title/url/score），自动识别对同一实体的不同表述/数值，按严重度排序输出冲突列表。
    llm: （v1.0.1 由 sync 定义生成，schema 字段描述完整）跨源事实冲突检测 (v1.8.1+ 第 9 工具)。输入多个来源（含 text/title/url/score），自动识别对同一实体的不同表述/数值，按严重度排序输出冲突列表。
parameters:
  - name: sources
    type: array
    required: true
    label:
      en_US: sources
      zh_Hans: sources
    human_description:
      en_US: 来源列表，每项含 text/title/url/score
      zh_Hans: 来源列表，每项含 text/title/url/score
    llm_description: 来源列表，每项含 text/title/url/score
    form: llm
  - name: subject
    type: string
    required: false
    label:
      en_US: subject
      zh_Hans: subject
    human_description:
      en_US: 调研主题（可选，用于过滤）
      zh_Hans: 调研主题（可选，用于过滤）
    llm_description: 调研主题（可选，用于过滤）
    form: llm
  - name: min_sources
    type: number
    required: false
    label:
      en_US: min_sources
      zh_Hans: min_sources
    human_description:
      en_US: 最少需要多少个来源才检测（默认 2）
      zh_Hans: 最少需要多少个来源才检测（默认 2）
    llm_description: 最少需要多少个来源才检测（默认 2）
    form: llm
  - name: max_conflicts
    type: number
    required: false
    label:
      en_US: max_conflicts
      zh_Hans: max_conflicts
    human_description:
      en_US: 最多返回多少个冲突（默认 20）
      zh_Hans: 最多返回多少个冲突（默认 20）
    llm_description: 最多返回多少个冲突（默认 20）
    form: llm
extra:
  python:
    source: tools/conflict_detection_async.py