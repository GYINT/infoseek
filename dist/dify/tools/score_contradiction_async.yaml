# 工具定义（v1.0.1 G8 自动生成，与 MCP TOOLS 对齐）
identity:
  name: score_contradiction_async
  author: infoseek
  label:
    en_US: score_contradiction_async
    zh_Hans: score_contradiction_async
  description:
    human:
      en_US: （v1.0.1 由 sync 定义生成，schema 字段描述完整）v3.0.0 GA 矛盾评分（v2.7.2 引入）。两句话矛盾评分，含 severity 等级。
      zh_Hans: （v1.0.1 由 sync 定义生成，schema 字段描述完整）v3.0.0 GA 矛盾评分（v2.7.2 引入）。两句话矛盾评分，含 severity 等级。
    llm: （v1.0.1 由 sync 定义生成，schema 字段描述完整）v3.0.0 GA 矛盾评分（v2.7.2 引入）。两句话矛盾评分，含 severity 等级。
parameters:
  - name: claim_a
    type: object
    required: true
    label:
      en_US: claim_a
      zh_Hans: claim_a
    human_description:
      en_US: 
      zh_Hans: 
    llm_description: 
    form: llm
  - name: claim_b
    type: object
    required: true
    label:
      en_US: claim_b
      zh_Hans: claim_b
    human_description:
      en_US: 
      zh_Hans: 
    llm_description: 
    form: llm
extra:
  python:
    source: tools/score_contradiction_async.py