# 工具定义（v1.0.1 G8 自动生成，与 MCP TOOLS 对齐）
identity:
  name: key_usage
  author: infoseek
  label:
    en_US: key_usage
    zh_Hans: key_usage
  description:
    human:
      en_US: Key 用量/成本报表（v1.0.1 新增）：基于 ~/.infoseek/key_usage.json 输出各 provider 调用数与估算成本。
      zh_Hans: Key 用量/成本报表（v1.0.1 新增）：基于 ~/.infoseek/key_usage.json 输出各 provider 调用数与估算成本。
    llm: Key 用量/成本报表（v1.0.1 新增）：基于 ~/.infoseek/key_usage.json 输出各 provider 调用数与估算成本。
parameters:
  - name: _empty
    type: string
    required: false
    label:
      en_US: _empty
      zh_Hans: _empty
    human_description:
      en_US: 占位
      zh_Hans: 占位
    llm_description: 占位
    form: llm
extra:
  python:
    source: tools/key_usage.py